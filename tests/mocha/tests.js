var assert = chai.assert,
    expect = chai.expect,
    should = chai.should;

suite('FoxNEOD', function () {

    setup(function () {
        console.log('Mocha setup');
    });

    suite('utils', function () {
        test('adfasdfasdf', function () {
        });
//        test('addPixelSuffix', function () {
//            strictEqual($f.utils.addPixelSuffix('12'), '12px', 'Adds the "px" suffix to a string passed in with no existing "px" in it.');
//            strictEqual($f.utils.addPixelSuffix(12), '12px', 'Adds the "px" suffix to a number passed in.');
//            strictEqual($f.utils.addPixelSuffix('30px'), '30px', 'Adds the "px" suffix to a string passed in that already has a "px" suffix.');
//        });
//
//        test('removePixelSuffix', function () {
//            strictEqual($f.utils.removePixelSuffix('12'), '12', 'Removes the "px" suffix to a string passed in with no existing "px" in it.');
//            strictEqual($f.utils.removePixelSuffix(12), '12', 'Removes the "px" suffix to a number passed in.');
//            strictEqual($f.utils.removePixelSuffix('30px'), '30', 'Removes the "px" suffix to a string passed in that already has a "px" suffix.');
//        });
    });

});